# Database URL
# For local development, you can use PostgreSQL or temporarily switch schema.prisma back to SQLite
# SQLite (local only - requires changing schema.prisma provider to "sqlite"):
# DATABASE_URL="file:./dev.db"
# PostgreSQL (recommended for both local and production):
DATABASE_URL="postgresql://username:your-secure-password@localhost:5432/mewogpt"
# For production with PostgreSQL (Railway, Vercel):
# DATABASE_URL will be automatically provided by your hosting platform

# OpenAI API Key
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY="your-openai-api-key-here"

# JWT Secret for authentication
# Generate a random secret string (e.g., using: openssl rand -base64 32)
JWT_SECRET="your-jwt-secret-here"

# Node Environment
NODE_ENV="development"
